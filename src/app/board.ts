import { Cell } from './cell';
import { Row } from './row';

export class Board{
    rows: Row[];
    
    /*
    getCell(row: number, column: number): Cell {
        //let row: Row = this.rows[row];
        //return this._bar;
    }
    */
    /*
    set bar(theBar:Boolean) {
        this._bar = theBar;
    }
    */
    /*
    function exclaim() {
        return "ouch!";
    }
    */
}